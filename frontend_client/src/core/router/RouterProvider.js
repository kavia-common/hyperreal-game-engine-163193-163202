import React from 'react';

/**
 * Simple placeholder router provider.
 * If react-router is added later, swap implementation here without touching the app.
 */
// PUBLIC_INTERFACE
export function RouterProvider({ children }) {
  /** Pass-through provider; replace with actual router when needed. */
  return children;
}
